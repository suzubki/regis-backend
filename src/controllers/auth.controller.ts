import { type Request, type Response } from "express"

class AuthController {
  static async signup(_req: Request, _res: Response) {
    try {
    } catch (error) {
    }
  }
}

export default AuthController
